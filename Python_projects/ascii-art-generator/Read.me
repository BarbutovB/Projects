# ASCII Art Generator

A simple Python tool that converts plain text into stylish ASCII art banners.

## Features
* Fast and lightweight.
* Uses the `art` library for high-quality ASCII fonts.
* Direct command-line interface.

## Requirements
* Python 3.x
* `art` library

## Installation
First, clone the repository and install the required library:
```bash
py -m pip install art
